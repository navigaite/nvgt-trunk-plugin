---
name: 📦 Draft new Release

run-name: '${{ github.workflow }} | ${{ github.actor }}'

on:
  workflow_dispatch:
    inputs:
      semver_increment:
        description: The version you want to release.
        required: true
        default: patch
        type: choice
        options:
          - major
          - minor
          - patch

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  action-man-draft-release:
    name: // Navigaite
    uses: navigaite/.github/.github/workflows/sub-draft-new-release.yaml@main
    secrets: inherit
    with:
      semver_increment: ${{ github.event.inputs.semver_increment }}
    permissions:
      contents: write
      pull-requests: write
